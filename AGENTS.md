# Repository Notes

- This is currently a documentation-only scaffold: there are no application sources, package manifests, CI workflows, or runnable build/test/lint commands.
- Treat `README.md` as the product and target-architecture brief, not as verification that the described React, Express, Supabase, Gemini, or Playwright implementation exists.
- `.gitignore` ignores all new files by default. Add explicit negation rules before introducing files that must be tracked.
